# Client / Reader Note — superseded

This document previously described the repository as a complete, verified reproduction and included expected/synthetic metric ranges. Those claims are retired because they were not backed by a traceable full-dataset measured run.

Use:

- `../README.md` for the current Persian run guide.
- `../REPRODUCIBILITY_AUDIT.md` for the scientific audit.
- `../reference/paper_targets.json` for values transcribed from the paper.
- `../outputs/measured/` only for actual model-run outputs.

A result from `outputs/reference/` or `outputs/smoke/` must never be presented as a measured simulation result.
