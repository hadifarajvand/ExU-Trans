# Comparison Summary — superseded

The previous version of this file claimed that all outputs were verified and matching. That claim was not supported by measured execution provenance and is withdrawn.

Use these files instead:

- `REPRODUCIBILITY_AUDIT.md` — detailed audit, defects, fixes, and remaining blockers.
- `reference/paper_targets.json` — table-specific values transcribed from the 2026 paper.
- `scripts/compare_with_paper.py` — comparison of **measured** CSV metrics against an explicit paper target.
- `README.md` — Persian execution guide.

Important: `outputs/reference` contains published reference values, not simulation results. Real model outputs belong under `outputs/measured`.
